# Data

This directory is for local job-search state such as the application tracker, follow-ups, pipeline entries, and scan history.

Runtime files in this directory are intentionally ignored by Git because they can contain personal job-search data.
