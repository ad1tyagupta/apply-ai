# Examples

These files are safe starter examples only.

Do not treat them as factual user data.

Suggested use:

- inspect the file structure
- replace examples with the user’s own uploads
- keep real user information in `intake/raw/`
